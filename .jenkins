pipeline {
    agent none

    environment {
        CCACHE_DIR = '/tmp/ccache'
        CCACHE_MAXSIZE = '10G'
        CCACHE_CPP2 = 'true'
        BUILD_JOBS = 8
        SITE = 'Jenkins'
    }
    stages {
        stage('Build') {
            parallel {
                stage('HIP') {
                    agent {
                        dockerfile {
                            filename 'Dockerfile.hipcc'
                            dir 'scripts/docker'
                            additionalBuildArgs ''
                            label 'rocm-docker && vega'
                            args '-v /tmp/ccache.kokkos:/tmp/ccache --device=/dev/kfd --device=/dev/dri --security-opt seccomp=unconfined --group-add video --env HIP_VISIBLE_DEVICES=$HIP_VISIBLE_DEVICES'
                        }
                    }
                    environment {
                        OMP_NUM_THREADS = 8
                        OMP_PLACES = 'threads'
                        OMP_PROC_BIND = 'spread'
                        LC_ALL = 'C'
                        PYTHON_VERSION = '3.8'
                        PYTHONPATH = '/usr/local/lib/python${PYTHON_VERSION}/site-packages:${PYTHONPATH}'
                    }
                    steps {
                        sh 'ccache --zero-stats'
                        sh 'echo "/opt/rocm/llvm/lib" > /etc/ld.so.conf.d/llvm.conf && ldconfig'
                        sh '''git clone -b develop https://github.com/kokkos/kokkos.git /tmp/kokkos-source && \
                              cmake -B /tmp/kokkos-build \
                                -DCMAKE_BUILD_TYPE=RelWithDebInfo \
                                -DCMAKE_CXX_COMPILER=hipcc \
                                -DCMAKE_CXX_STANDARD=17 \
                                -DKokkos_ENABLE_HIP=ON \
                                -DKokkos_ENABLE_OPENMP=ON \
                                /tmp/kokkos-source'''
                        sh 'cmake --build /tmp/kokkos-build --target all --parallel ${BUILD_JOBS}'
                        sh 'cmake --build /tmp/kokkos-build --target install --parallel ${BUILD_JOBS}'
                        sh 'python3 -m pip install -r requirements.txt && python3 -m pip install pytest'
                        sh '''cmake -B build \
                                -DCMAKE_BUILD_TYPE=RelWithDebInfo \
                                -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
                                -DCMAKE_CXX_COMPILER=hipcc \
                                -DENABLE_LAYOUTS=ON \
                                -DENABLE_MEMORY_TRAITS=OFF \
                                -DENABLE_EXAMPLES=ON \
                                -DENABLE_WERROR=ON \
                                -DENABLE_CTP=ON \
                                -DPython3_EXECUTABLE=$(which python3)'''
                        sh 'cmake --build build --target all --parallel ${BUILD_JOBS}'
                        sh 'cmake --build build --target analyze-pykokkos-base-compile-time'
                        sh 'cmake --build build --target install --parallel ${BUILD_JOBS}'
                        sh 'cd build'
                        sh 'PYTHONPATH=${PWD}:${PYTHONPATH} pytest .'
                        sh 'PYTHONPATH=${PWD}:${PYTHONPATH} python3 ./ex-numpy.py'
                        sh 'cd /tmp && python3 -c "import kokkos; print(kokkos.__file__)"'
                    }
                    post {
                        always {
                            sh 'ccache --show-stats'
                        }
                    }
                }
                stage('CUDA') {
                    agent {
                        dockerfile {
                            filename 'Dockerfile.nvcc'
                            dir 'scripts/docker'
                            additionalBuildArgs ''
                            label 'nvidia-docker && volta'
                            args '-v /tmp/ccache.kokkos:/tmp/ccache --env NVIDIA_VISIBLE_DEVICES=$NVIDIA_VISIBLE_DEVICES'
                        }
                    }
                    environment {
                        OMP_NUM_THREADS = 8
                        OMP_PLACES = 'threads'
                        OMP_PROC_BIND = 'spread'
                        NVCC_WRAPPER_DEFAULT_COMPILER = 'g++-8'
                        PYTHON_VERSION = '3.8'
                        PYTHONPATH = '/usr/local/lib/python${PYTHON_VERSION}/site-packages:${PYTHONPATH}'
                    }
                    steps {
                        sh 'ccache --zero-stats'
                        sh 'rm -rf build'
                        sh '''git clone -b develop https://github.com/kokkos/kokkos.git /tmp/kokkos-source && \
                              cmake -B /tmp/kokkos-build \
                                -DCMAKE_BUILD_TYPE=RelWithDebInfo \
                                -DCMAKE_CXX_COMPILER=g++-8 \
                                -DCMAKE_CXX_STANDARD=17 \
                                -DKokkos_ENABLE_OPENMP=ON \
                                -DKokkos_ENABLE_CUDA=ON \
                                -DKokkos_ENABLE_CUDA_LAMBDA=OFF \
                                -DKokkos_ENABLE_CUDA_UVM=ON \
                                -DKokkos_ENABLE_CUDA_RELOCATABLE_DEVICE_CODE=ON \
                                /tmp/kokkos-source'''
                        sh 'cmake --build /tmp/kokkos-build --target all --parallel ${BUILD_JOBS}'
                        sh 'cmake --build /tmp/kokkos-build --target install --parallel ${BUILD_JOBS}'
                        sh 'python3 -m pip install -r requirements.txt && python3 -m pip install pytest'
                        sh '''cmake -B build \
                                -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
                                -DCMAKE_BUILD_TYPE=RelWithDebInfo \
                                -DCMAKE_C_COMPILER=gcc-8 \
                                -DCMAKE_CXX_COMPILER=g++-8 \
                                -DCMAKE_CXX_STANDARD=17 \
                                -DENABLE_LAYOUTS=ON \
                                -DENABLE_MEMORY_TRAITS=OFF \
                                -DENABLE_EXAMPLES=ON \
                                -DENABLE_TIMING=ON \
                                -DENABLE_WERROR=ON \
                                -DENABLE_CTP=ON \
                                -DPython3_EXECUTABLE=$(which python3)'''
                        sh 'cmake --build build --target all --parallel ${BUILD_JOBS}'
                        sh 'cmake --build build --target analyze-pykokkos-base-compile-time'
                        sh 'cmake --build build --target install --parallel ${BUILD_JOBS}'
                        sh 'cd build'
                        sh 'cat nvcc-compile-time.csv'
                        sh 'PYTHONPATH=${PWD}:${PYTHONPATH} pytest .'
                        sh 'PYTHONPATH=${PWD}:${PYTHONPATH} python3 ./ex-numpy.py'
                        sh 'cd /tmp && python3 -c "import kokkos; print(kokkos.__file__)"'
                    }
                    post {
                        always {
                            sh 'ccache --show-stats'
                        }
                    }
                }
            }
        }
    }
}
